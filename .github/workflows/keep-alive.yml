name: Keep Railway Alist Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Alist to prevent sleep
        run: curl -L --max-time 15 "https://alist-production-8dae.up.railway.app"
