name: Keep Railway Alist Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # 每 5 分钟一次
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Alist to prevent sleep
        run: curl -I --max-time 10 "https://alist-production-8dae.up.railway.app"
